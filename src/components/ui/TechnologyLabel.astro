---

const { 
    name, 
    logo 
} = Astro.props
---
{logo && (
    <div class="tech-item">
        <img src={logo} alt={`${name} logo`} class="tech-logo" />
    </div>
)}

<style>
    .tech-item {
        @apply flex items-center justify-center
    }

    .tech-logo {
        @apply w-6 h-6
    }
</style>